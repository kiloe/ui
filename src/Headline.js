import Title from './Title';

// Headline is for "page" headings (not crazy big banners like Display nor Titles of things in the page)
// ...see Title
export default class Headline extends Title {
  static className = 'headline'
  static scale = 2.4;
}

